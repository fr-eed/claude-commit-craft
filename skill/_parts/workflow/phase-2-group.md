# Phase 2: Group changes by logical concern

Cluster files (and hunks if needed) into proposed commits. Prefer fewer commits when changes are tightly coupled. Reverse-test: reverting any single proposed commit must leave the codebase buildable and coherent.
